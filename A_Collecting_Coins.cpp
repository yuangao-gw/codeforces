#include <bits/stdc++.h>
int main(int argc, char const *argv[])
{
    int n;
    std::cin >> n;
    for (size_t i = 0; i < n; i++)
    {
        std::vector<int> a(4);
        for (size_t i = 0; i < a.size(); i++)
        {
            std::cin >> a[i];
        }
    }
    return 0;
}
